/*
  关键字 declare 表示声明的意思，我们可以用它来做出各种声明
  declare 是给编译器读取用的，可以暂时不调用
  使用declare定义的内容，全局都可以使用，无需在引入。
  
  declare var // 声明全局变量
  declare function // 声明全局方法
  declare class // 声明全局类
  declare enum // 声明全局枚举类型
  declare namespace // 声明（含有子属性的）全局对象
  interface 和 type // 声明全局类型

*/
let arraynumber: ArrayNumber = [1, 2, 3, 4, 5];
console.log(age);
new Person("name");
$.ajax("/login", "post", {});
$.userName;
$.getName.onClick();
